"""strata-mcp — academic research engine as a standalone MCP server.

A per-research-project SQLite library of papers analysed in two rounds, gap
analysis, drafts and an arXiv / Semantic Scholar scout. Claude Code is the
reasoning model; strata-mcp only stores data, fetches papers and runs searches.
No remote LLM, no API keys.
"""

__version__ = "0.1.0"
